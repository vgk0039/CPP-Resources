// A function calling itself is called reccursion
#include <bits/stdc++.h>
using namespace std;
int main()
{

   /*Recursion in C++ is a programming technique where a function calls itself directly or indirectly to solve a problem.

It’s used when a big problem can be broken down into smaller sub-problems that are similar in nature.

*/
   //------------SYNTAX-----------
   /*
   void recursiveFunction() {
       // Base Case (exit condition)
       if (condition)
           return;

       // Recursive Case (function calls itself)
       recursiveFunction();
   }

   */

   //Base Case:	Condition that stops recursion (must exist to prevent infinite loop)
   //Stack Frame	Every recursive call is stored in the call stack, and popped when the function returns
   

   return 0;
}